export class Colors {
  name: string
  firstColor: string
  secondColor: string

  constructor(name: string, firstColor: string, secondColor: string) {
    this.name = name
    this.firstColor = firstColor
    this.secondColor = secondColor
  }
}
